# 环境与安装指引（第一步配套文档）

> 生成时间：2026-09-18 ｜ 依据：`docs/plan/01-GitHub学习复现项目清单.md` 第七节 14 天起步清单
> 本文件记录：**环境实测结果 + 24 小时内待装组件 + GitHub 建库推送步骤 + 今晚任务卡**。

## 一、环境实测结果（2026-09-18 实测）

| 组件 | 状态 | 版本 / 说明 |
| --- | --- | --- |
| git | ✅ 已装 | 2.55.0（含 Git Credential Manager，推 GitHub 时会自动弹浏览器登录） |
| Python | ✅ 已装 | 3.12.10（pip 25.0.1） |
| Node.js | ✅ 已装 | v24.19.0 |
| VS Code | ✅ 已装 | 1.137.0（**Cursor 未装**，VS Code 现阶段够用） |
| winget | ✅ 可用 | 所有安装都能用 winget 一条命令完成 |
| 磁盘空间 | ✅ 充足 | C: 剩 178GB（装 MySQL/Docker 放 C 即可） |
| **MySQL** | ✅ 已装 | 8.4.9（09-18 完成：服务 `MySQL84` 开机自启，端口 3306，`SELECT 1` 已通过；root 密码见本机备忘文件 `C:\Users\19795\求职\⚠️MySQL密码备忘-勿提交GitHub.txt`） |
| **Docker Desktop** | ❌ 未装 | 待装（约定 SQL 母网闯完 5 关后），需要 WSL2 + 一次重启（见 2.3） |
| Cursor | ❌ 未装 | 可选，VS Code 先用着 |
| GitHub 账号 | ❌ 未注册 | 推送远程仓库前需要（见第三节） |

## 二、24 小时内安装清单（命令实测 winget 源存在）

> ✅ **09-18 已完成：MySQL 8.4.9 + DBeaver 26.2.0**（2.1、2.2 已不用再做）。**只剩 2.3 Docker Desktop 待装**（约定 SQL 母网闯完 5 关后）。

> 装之前先关掉正在跑的其他安装程序。以下命令建议**自己开一个 PowerShell 窗口**逐条跑（能看到进度和报错）。

### 2.1 MySQL 8（第 1 站基础）✅ 已于 09-18 完成

```powershell
winget install --id Oracle.MySQL -e --accept-package-agreements --accept-source-agreements
```

- 装的是 **MySQL Installer**，会弹出图形界面：选 **MySQL Server + MySQL Workbench**（其余组件不用勾）。
- 界面里会让你设 **root 密码 —— 务必记住**（建议记在本地密码管理器，**不要写进本仓库**）。
- winget 源当前版本是 8.4 LTS；`wonderful-sql` 按 8.0 编写，8.4 完全兼容（`SELECT 1` 起步无差别）。
- **验收**：装完开 PowerShell 跑 `mysql -u root -p`，输入密码后执行 `SELECT 1;` 能返回 `1` 即通过。
  - 若提示「mysql 不是内部或外部命令」：去「编辑系统环境变量 → 环境变量 → Path」加上 MySQL 的 bin 目录（默认 `C:\Program Files\MySQL\MySQL Server 8.4\bin`），重开终端即可。

### 2.2 客户端：DBeaver（免费社区版）✅ 已于 09-18 完成

```powershell
winget install --id DBeaver.DBeaver.Community -e --accept-package-agreements --accept-source-agreements
```

- 免费、跨平台、连 MySQL 零配置；连本地 MySQL：`localhost:3306` + root 密码即可。
- 备选：**Navicat Premium Lite 17 已免费**，但 winget 上没有，需从 **Microsoft Store** 装（搜 `Navicat Premium Lite`）。二选一即可，建议 DBeaver。

### 2.3 Docker Desktop（第 1 周起 `sql-tutorial` / `pagila` 都要用）

```powershell
wsl --install          # 若提示需要 WSL2；可能要求重启
winget install --id Docker.DockerDesktop -e --accept-package-agreements --accept-source-agreements
```

- 装完**重启电脑**，首次打开 Docker Desktop 点 **Accept**，等右下角鲸鱼图标变绿色（Engine running）。
- **验收**：PowerShell 跑 `docker run --rm hello-world` 输出 `Hello from Docker!` 即通过。
- ⚠️ 这一步最耗时间（下载 1GB+ + 重启），**可以放到今晚闯完 5 关之后**，或明早做。

> ### 📌 Docker 安装断点存档（09-20 暂停，改天续装）
> 已完成：✅ WSL 功能 + 虚拟机平台（VirtualMachinePlatform）**已启用**（`RestartNeeded=True`，**重启一次后生效**，这台机器还没重启过）
> 未完成：⬜ WSL2 内核 MSI（下载中断）⬜ Docker Desktop（winget 下载中断）
> **续装顺序**：① 重启电脑 → ② 下载内核 `https://aka.ms/wsl2kernel` → ③ 管理员运行 `msiexec /i wsl_update_x64.msi /qn`（静默）→ ④ `winget install --id Docker.DockerDesktop -e` → ⑤ 启动 Docker Desktop，点 Accept，等引擎绿 → ⑥ `docker run --rm hello-world` 验收
> ⚠️ 若重启后 `wsl --status` 报「虚拟化未启用」：进 BIOS（开机 F2/Del）把 SVM/Intel VT 打开（Advanced → CPU Configuration）。

### 2.4 Cursor（可选，AI 编程助手）

```powershell
winget install --id Anysphere.Cursor -e --accept-package-agreements --accept-source-agreements
```

- 计划第 2 周要记录「AI 辅助写 SQL」留档，Cursor 是顺手工具；但 **VS Code 已装且够用**，装不装自己定。

## 三、GitHub 账号注册 + 建库 + 推送（远程仓库这一步）

> 本仓库本地骨架已完成，**只差远程**。你还没有 GitHub 账号，按下面三步走（全程 15 分钟）：

1. **注册**：打开 https://github.com/signup ，建议邮箱用 `1979542038@qq.com`，用户名自己想一个（会出现在链接里，建议 `拼音-数字` 风格）。
2. **建库**：登录后点右上角 `+` → `New repository`：
   - Repository name：`data-career-roadmap`
   - **Public**（要的就是绿色格子被面试官看到）
   - **不要勾** `Add a README file`（我们已有 README，勾了反而冲突）
   - 点 `Create repository`
3. **推送**：把下面三条命令里的 `<你的用户名>` 换成你的 GitHub 用户名，在仓库目录 `C:\Users\19795\求职\data-career-roadmap` 打开 PowerShell 执行：

```powershell
git remote add origin https://github.com/<你的用户名>/data-career-roadmap.git
git branch -M main
git push -u origin main
```

- 第一次 push 会弹 **Git Credential Manager 浏览器窗口**，点授权登录即可（本机 git 已带此组件）。
- ⚠️ **网络排障（09-18 实测）**：git 默认**不读 Windows 系统代理**，直连 github.com:443 会超时（`Failed to connect to github.com:443`）。本机已配置（代理软件开着才有用）：
  ```powershell
  git config --global http.proxy  http://127.0.0.1:15591
  git config --global https.proxy http://127.0.0.1:15591
  ```
  若某天 push 又超时：先确认代理软件（Clash 类）**正在运行**；代理没开时可用 `git config --global --unset http.proxy`（及 https.proxy）临时直连。push 前可用 `git ls-remote https://github.com/<你的用户名>/data-career-roadmap.git` 自测连通性（有输出或安静退出=通；报 Repository not found=没建库；超时=代理没开）。
- 或者：用 **VS Code** 打开仓库文件夹，点左侧源代码管理 → 首次推送时按提示用浏览器登录 GitHub，一样效果。
- push 成功后：仓库页 `Settings → GitHub Pages` 不用开（纯 md 仓库），直接确认 `Contribution activity` 出现绿色格子即可。
- **以后每天**：学完一条内容就 `git add -A && git commit -m "log: 09-xx <内容>" && git push`（或用 VS Code 图形界面点提交+同步）。

## 四、今晚任务卡（09-18，40 分钟）

| # | 动作 | 产出 | 说明 |
| --- | --- | --- | --- |
| 1 | 浏览器打开 **http://sqlmother.yupi.icu** | 闯关截图 | 程序员鱼皮开源 SQL 闯关站，零安装即时反馈 |
| 2 | **闯前 5 关**（进度条到 5/xx） | 5 关通过 | 遇到不会的直接看提示，目标是「手感」不是「全会」 |
| 3 | 截图存到 `log/assets/`（每关一张或最后一张进度截图） | 图片证据 | Win + Shift + S 截屏；图片控制在 500KB 内（大了用画图压缩） |
| 4 | 提交打卡 | commit | `git add -A && git commit -m "log: 09-18 SQL母网 5 关"` |
| 5 | 回血 | 30–60 分钟娱乐 | **硬红线，不可省略** |

> 闯关完顺手把 `log/2026-09.md` 里 09-18 行的「学习时长」「产出」两列填上。
