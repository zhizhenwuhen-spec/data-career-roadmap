# 自己手写的 SQL

> ⚠️ **只放自己写的 SQL**。`wonderful-sql`、`BigData-Notes` 等仓库**无许可证**，其题目原文、答案、教材内容**一律不得复制进本仓库**（见根目录 README 第六节「引用红线」）。抄写练习时请在**自己的话复述题意**之后，只保留自己的解法。

## 命名规范

```
sql/
├── day01-select-basics.sql        # 按天：dayNN-主题.sql
├── day02-groupby-having.sql
├── wsql-ch03-ex01.sql             # 按教材章节做题：wsql-chNN-exNN.sql
├── nk-sql-1024-top3-per-dept.sql  # 牛客/力扣：平台-题号-关键词.sql
└── self-orders-join-05.sql        # 自出题：self-<主题>-序号.sql
```

## 每题的文件结构（模板）

```sql
-- ============================================================
-- 题目：按部门分组，取薪资 Top3（自己用中文复述，不抄原文）
-- 来源：自出题 05 ｜ 数据库：sql-tutorial（--size small，75 万行）
-- 日期：2026-xx-xx ｜ 用时：xx 分钟（限时手写）
-- ============================================================

-- 【我的思路】先写思路再写 SQL（面试口述时就是这段）
-- 1. 用窗口函数按部门分区、按薪资倒序编号，外层筛 rn <= 3

-- 【我的答案】
SELECT ...
FROM ...
;

-- 【校验记录】
-- EXPLAIN 结果：无全表扫描 / 使用了 idx_xxx
-- 行数核对：COUNT(*) = ???，与预期一致
-- 反例测试：部门人数 < 3 时是否仍正确返回；薪资并列时 RANK/DENSE_RANK 的差异

-- 【AI 辅助记录】（如有）见 ../../docs/ai-assisted-sql.md 第 N 条
```

## 每天收尾自检

- [ ] 能不能**合上屏幕**把这道题的逻辑用中文讲一遍？
- [ ] 有没有做**校验动作**（行数 / 反例 / EXPLAIN / 对拍）？只跑通不算过。
- [ ] 抄写型题目有没有混入原教材文本？（有 → 立刻删）
